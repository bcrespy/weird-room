import Color from '@creenv/color';

let config = {
  // minimum value the distorsion of the perlin can ben applied
  distortionMin: 0,

  // the range of the distorsion that will be added depending on the energy strength
  distortionRange: 3,

  scale: 0.0,

  cameraCycle: 60000,

  torusDeform: 1.5
};


export default config;